# HotKey Web 本地环境变量配置

# Next.js 服务端将同源 /api、/healthz 请求代理到该后端地址。
# 该变量仅供 next.config.ts 使用，不会暴露给浏览器。
HOTKEY_API_ORIGIN=http://127.0.0.1:8080

# 可选：Docker/生产构建使用 standalone，本地开发留空。
NEXT_OUTPUT=

# 可选：docker compose 对外暴露的 Web 端口。
WEB_PORT=3000
