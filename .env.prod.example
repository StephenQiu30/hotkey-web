# HotKey Web Docker 生产环境模板
# 默认无需填写任何变量，复制为 .env.prod 后即可使用生产 Compose 命令启动。

# 可选：后端不在宿主机 8080 端口时取消注释并修改。
# 该地址仅由 Next.js 服务端代理使用，不会暴露给浏览器。
# HOTKEY_API_ORIGIN=http://host.docker.internal:8080

# 可选：Web 容器对外端口；不配置时为 3000。
# WEB_PORT=3000
