# HotKey Web Docker 生产部署配置

# 区分镜像标签和容器内的部署环境；Next.js 仍使用 NODE_ENV=production。
HOTKEY_DEPLOY_ENV=prod

# 区分不同环境的 Compose 网络、容器与其他资源。
COMPOSE_PROJECT_NAME=hotkey-web-prod

# Next.js 服务端代理访问的后端地址。
HOTKEY_API_ORIGIN=http://host.docker.internal:8080

# Web 容器映射到宿主机的端口。
WEB_PORT=3000
