version: "3.9"

services:
  hotkey-web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        HOTKEY_API_ORIGIN: ${HOTKEY_API_ORIGIN:-http://host.docker.internal:8080}
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  # Optional: start the Go backend alongside the web app.
  # Uncomment and adjust the build context path if you have hotkey-server
  # in a sibling directory.
  #
  # hotkey-server:
  #   build:
  #     context: ../hotkey-server
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - DB_DSN=postgres://hotkey:hotkey@db:5432/hotkey?sslmode=disable
  #     - REDIS_ADDR=redis:6379
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_started
  #   restart: unless-stopped
